# Jupyter Book

[Jupyter Book](https://jupyterbook.org/stable/) 是基于 Jupyter Notebook 构建交互式书籍的工具。

安装：

```bash
pip install jupyter-book
```

初始化项目：

```bash
jupyter book init
```

构建项目：

```bash
jupyter book build
```

启动项目：

```bash
jupyter book start
```
